import type { Address } from '../types';

/**
 * Placeholder for future official Volt contract addresses.
 * Consumers should currently provide their own deployed address to the VoltClient.
 */
export const voltContractAddresses: Record<number, Address> = {
  // 1: '0x...', // Mainnet placeholder
  // 11155111: '0x...', // Sepolia placeholder
};
